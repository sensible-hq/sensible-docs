name: Linting and link checking
on: [push]

jobs:
  check-links:
    runs-on: ubuntu-latest
    steps:
      run: echo "hello world"
      # - name: Checkout 🛎️
      # uses: actions/checkout@v2
      
      # - name: Set up Ruby 💎
      # uses: ruby/setup-ruby@v1
      # with:
      #   ruby-version: '2.7'
      #   bundler-cache: true
        
      #   - name: Install HTML Proofer 🔧
      #   run: gem install html-proofer
        
      #   - name: hello

  prose:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Vale
        uses: errata-ai/vale-action@master
        with:
          # Optional
          styles: |
            https://github.com/errata-ai/Google/releases/latest/download/Google.zip
            https://github.com/errata-ai/write-good/releases/latest/download/write-good.zip

          # Optional
          # config: https://raw.githubusercontent.com/errata-ai/vale/master/.vale.ini

          # Optional
          files: __onlyModified #all

      env:
        # Required
        GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}

    
        
        
        # - name: Build markdown docs and check links🏗
  #         # TODO  https://michaelcurrin.github.io/code-cookbook/recipes/ci-cd/github-actions/workflows/check-links/html-proofer-generic.html 
  #         # run: bundle exec jekyll build --trace
  #         # TODO: Supply your own build command for the Build step. And point the proofer tool at your build output directory, like build or out.
            # TODO: can i just run ../check_links/check_links.rb ??? might have to break it up a bit? 
  #       - name: Check for broken links 🧐🔗
  #         run: htmlproofer --log-level :debug _site 2> links.log

  #       - name: Archive checker log 🗄
  #         uses: actions/upload-artifact@v1
  #         with:
  #           name: links-check.log
  #           path: links.log   
name: Dev Style images

## Run workflow for every push that 1. affects the dir assets  2. is to non-main branch
# bad idea to run this actually b/c you end up with image merge conflicts on the PR
# on:
#   push:
#     branches-ignore:
#       - main
#     paths:
#       - 'readme-sync/assets/v0/images/screenshots/**'

# jobs:
#   images:
#     runs-on: ubuntu-latest

#     steps:
#       - name: checkout repo
#         uses: actions/checkout@v3
#       - name: update packages
#         run: sudo apt update
#       - name: Install ghostscript for ImageMagick
#         run: sudo apt install ghostscript
#       - name: run ImageMagick
#         run: bash ./scripts/style_images.sh
#       - name: auto commit changes from this workflow
#         uses: stefanzweifel/git-auto-commit-action@v4
#         with:
#           commit_message: "automated image style commits"

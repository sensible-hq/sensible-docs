# blog-short skill: open items

## Open

- [ ] **LLM vs. deterministic framing in "The Solution"** — The longer blog template has a well-developed framing for when Sensible uses LLM-based methods (Query Group, NLP Table) vs. deterministic layout configs, and why. This skill's template doesn't surface that distinction yet. Add a Paragraph 2 variant or a dedicated framing block that explains the LLM vs. deterministic tradeoff in terms of the document type's structural properties — not framed as "manual vs. automatic" (that's outdated) but as the real choice: LLM methods for variable/unstructured docs vs. layout-anchored methods for consistent, high-volume formats.

- [ ] **Config library links when doc type is supported** — When the document type has a prebuilt config in the library, the template should include a link to https://www.sensible.so/configuration-library. Figure out where in the post this link belongs (likely the Conclusion or a short callout at the end of "The Solution") and add the pattern to the template.

## Completed

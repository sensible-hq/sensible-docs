# blog-parse session checklist

- [x] Add canonical comments to `json5-comments-reference.md` for SenseML params missing from delivery orders post: `flags`, `horizontalAnchor`, `matches`, `pattern`, `wordFilters`
- [x] Template: add new sections from commission statements post (delivery orders and insurance quotes sections already done)
- [x] Use Notion MCP server to push blog post drafts (and revisions) to Notion
- [x] After generating the draft, upload the config and PDF using `scripts/upload_pr_extractor.py` and run live extraction before drafting (Step 4 in SKILL.md)
- [x] In Step 3 (config analysis): summarize ALL fields in the config before selecting examples
- [x] In Step 3 (config analysis): provide explicit reasoning for field selection
- [x] After Step 3 field inventory, pause for user feedback before Step 4
- [x] Add a blog post step for explaining fingerprints when present (Step 5 in SKILL.md; prereq JSON5 comments done)
- [x] Write `test_blog_output.py`: extracts the "Putting it all together" code block from a draft, runs it via the Sensible API against the golden PDF, and diffs the result against the output block in the draft — prints a log of matches/mismatches for review before publishing (layout-based configs are deterministic; flag LLM-based fields separately)
- [x] Add a "Putting it all together" section (template + delivery orders draft done)
- [x] Cross-check individual output blocks against combined output (Step 6.6 in SKILL.md)
- [ ] Break the skill into sub-skills: `SKILL.md` is long and monolithic — investigate splitting it into composable parts (e.g. field-inventory, draft-writer, enrichment, publish) that the top-level skill orchestrates. Evaluate whether sub-skills should run as subagents, and whether different steps warrant different models (e.g. a cheaper/faster model for deterministic steps like comment dedup and config sync vs. a more capable model for judgment steps like field selection and intro framing).
- [x] Write `OVERVIEW.md` in the skill directory: a human-readable summary of what the skill does and how it works, covering each step at a glance and calling out which steps are deterministic (fetch config, run extraction, dedup comments, sync combined config, publish to Notion) vs. judgment-dependent (field selection, intro framing variant, line count trimming). Keep it separate from `SKILL.md` — that file is the operative instruction set for Claude, not documentation for humans. The YAML frontmatter `description` field is a one-liner; don't put the summary there.
- [ ] When abbreviating a Sections field for the blog post, fields are atomic: either include a field exactly as it appears in the original config, or delete it entirely. No partial edits, no trimming within a field, no simplifying its anchor or method. The structural wrapper (`type: sections`, `range`, `anchor`, `stop`, `fields`) is also verbatim and untouchable. Past failure: partially trimmed a field's internals, causing the blog config to silently differ from the original and produce wrong output.
- [ ] In Step 1 of the skill: if the user provides a config URL/path and PDF path, write a Python script to download both to `drafts/blog-[slug]-config-source.json` and `drafts/blog-[slug].pdf` respectively using the Sensible API (for configs) or direct file copy (for local PDFs), then update the meta file with the local paths. This way all artifacts are in the repo alongside the draft and future sessions don't depend on the user's local Downloads folder or a remote URL.
- [x] Persist extraction artifacts alongside each draft: after Step 6.5, write a `drafts/blog-[slug]-meta.json` with the PDF path, combined config path, and the "View in Sensible app:" URL so future sessions don't need to re-run the upload to recover the URL.
- [x] Add canonical comments to `json5-comments-reference.md` for `checkbox` and `pickValues` — neither is currently covered.
- [ ] **PARTIAL** `"type": "sections"` comment in `json5-comments-reference.md` still says `/* extracts repeating sections; returns each section as an object */`. The appraisal draft uses `/* scopes child fields to a defined region of the document; returns each section as an object */` for non-repeating use. Decide: update the reference to the new wording (works for both cases), or add a second variant for non-repeating use.
- [ ] **PARTIAL** Sections-for-non-repeating-data explanation is in the appraisal draft (property rights section) but not yet in `blog-post-template.md`. Approved prose in the draft: "The Sections method can extract repeating data — like line items in a table — but it's also useful for scoping queries to a single, well-defined region of a complex document." Add this to the template when doing the template update.
- [ ] **Pretty-print all SenseML code blocks** — no compact single-line objects (`{ "key": "val", ... }`). Every object and array gets its own lines, even if it's longer. This applies during drafting (Step 5) and after json5-commenter (Step 6) — comments on object-opening lines (e.g. `"match": { /* comment */`) require multi-line format to be readable.
- [ ] Add inline doc links in blog post prose for each SenseML method demonstrated — e.g. "the [Region method](https://docs.sensible.so/...)" on first mention. Links should point to the published reference page for that method. Do not link in code block comments, only in prose.
- [ ] No repeated comments within a single code block: once a parameter, method, or enum value has been commented in a code block, do not repeat that comment later in the same block. E.g. if `"type": "startsWith"` is commented on the first anchor, every subsequent `"type": "startsWith"` in that block gets no comment. This applies to all repeated parameters (`"type"`, `"id"`, `"position"`, `"match"`, etc.) — comment on first use only.
- [ ] Update `blog-post-template.md`: (a) add the 75-line code block limit rule (property rights appraised ~73 lines = calibration ceiling; trim sections to 1-2 illustrative sub-fields; deeply nested sections = 1 sub-field max); (b) show the final three-paragraph intro structure (P1 description + problem → P2 "With Sensible..." boilerplate with "hybrid deterministic and LLM-based" → P3 lead-in + LLM/deterministic framing); (c) add nested-sections constraint to field selection guidance. Compare appraisal Draft v1 vs Draft v4 in Notion first to inform (b): Draft v1: https://app.notion.com/p/38ac7dd4978881eba82ccd8c8819cf4d — Draft v4: https://app.notion.com/p/38ac7dd497888161b4b4e32c11c64249. See `intro-rewrite-feedback.md` for the verbatim accepted intro example.
- [ ] Add blog-specific field selection guidance: avoid demonstrating Sections fields whose sub-fields themselves use Sections (nested sections). The structural wrapper overhead (~40+ lines per level) consumes the code block budget before any field logic appears, and the nesting is too complex to explain concisely in a blog post. Prefer flat top-level fields or single-level Sections. Appraisal draft: dropped `sales_comparison` (nested sections within sections) entirely after failed attempts to trim it. Add this as a constraint in Step 3 field selection in SKILL.md.
- [ ] Add blog explanation (and template note) for why Sections is used for non-repeating data. In the appraisal config, `subject` and `reconciliation` are both Sections fields but neither repeats. Use the following canonical framing (approved wording):
  - **Organize extracted data into hierarchical object schemas.** For example, an appraisal document contains a single `Reconciliation` heading with non-repeating data. You create a section for the heading so that you can nest its extracted data in a reconciliation object — for example, `reconciliation.final_value` and `reconciliation.as_is.appraisal_condition`.
  - **Increase extraction accuracy for complex documents.** Complex documents can contain data in one area (for example, repeated text) that interferes with queries in another area. Sections isolate different areas of the document from each other so that you can author simpler, more robust queries in each area.
  - The blog post should surface whichever reason applies to the demonstrated field. `reconciliation` fits the schema-organization reason; `subject` fits the accuracy/isolation reason. Don't say Sections is only for repeating data — that's the most common reader assumption and it's wrong.
